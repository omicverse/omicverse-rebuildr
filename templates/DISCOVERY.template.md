# Discovery — py-<PkgName>

> Committed BEFORE any algorithmic code. Records the decision to start this port
> and which omicverse-org Python ports get reused as dependencies.
>
> Generated by `python -m engine.discover_omicverse_deps`.

## 1. Is this package already ported?

Output of `python -m engine.discover_omicverse_deps --check <PkgName>`:

```
[paste output here]
```

**Decision**: <START_PORT | STOP — duplicate of existing repo X>

## 2. Dependency audit

Output of `python -m engine.discover_omicverse_deps --description <pkg>-ref/DESCRIPTION`:

```
[paste output here]
```

## 3. Decisions per R dep

For each R dep, record `hard | optional | native-python | out-of-scope`:

| R dep | omicverse match (if any) | Decision | Python equivalent | Note |
|---|---|---|---|---|
| `<dep1>` | `py-<X>` | hard dep | `pymclustR>=0.1` | reuse |
| `<dep2>` | — | native-python | `scipy.spatial` | no port needed |
| `<dep3>` | — | out-of-scope | — | plotting / GUI |
| `<dep4>` | — | future-port | (none yet) | scope for v0.2 of omicverse roadmap |

## 4. Reusable work saved

| Reused omicverse port | Approx. LOC saved | Notes |
|---|---|---|
| `py-<X>` | ~3000 | reuses HC + EM kernels, BIC scan |
| ... | ... | ... |

**Total**: ~XXXX LOC and ~Y person-weeks of work saved by reuse.

## 5. New ports surfaced

R deps that have no omicverse mirror AND are worth porting in the future:

- `<dep>` — `<one-line reason>`. Suggested rank in roadmap: <T1/T2/T3>.

(Add these to `omicverse-rebuildr/examples/ROADMAP_<DOMAIN>.md` as new entries.)
